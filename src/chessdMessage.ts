import { basis } from 'basisData';
/**
 * 
 */
class ChessMessage extends basis {
    coord: any;

    constructor(){
        super();
        // this.coord = super.piecesCoordinate(wid, hig);
    }
}